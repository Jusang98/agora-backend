extends base

block content 
    form(method="POST")
        input(placeholder="E-mail", type="email", name="user_email", required)
        input(placeholder="Password", type="password", name="user_pwd", required)
        input(type="submit", value="로그인")